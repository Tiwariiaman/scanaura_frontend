import 'package:flutter/material.dart';

class AiImportScreen extends StatelessWidget {
  const AiImportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AI Menu Import'),
    );
  }
}